package laughinglen.repository;

import org.h2.jdbcx.JdbcConnectionPool;
import org.junit.Test;

import static org.hamcrest.CoreMatchers.equalTo;
import static org.junit.Assert.*;

public class RepositoryTest {

	@Test
	public void shouldCreateModule()	{
//		final DataSourceModule dataSourceModule = new DataSourceModule(JdbcConnectionPool.create("jdbc:h2:mem:test", "user", "password"));
//		final Repository r1 = Dagger_Repository.builder().dataSourceModule(dataSourceModule).build();
//		final Repository r2 = Dagger_Repository.builder().dataSourceModule(dataSourceModule).build();
//
//		assertNotNull(r1.dataSource());
//		assertNotNull(r2.dataSource());
//		assertThat(r1.dataSource(), equalTo(r2.dataSource()));
	}

}